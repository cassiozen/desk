<% if activity.trackable %>
  <% if activity.trackable.state == "open" %>
    reopened issue <%= activity.trackable.issue.subject %>
  <% elsif activity.trackable.state == "pending" %>
    put on hold issue <%= activity.trackable.issue.subject %>
  <% elsif activity.trackable.state == "closed" %>
    closed issue <%= activity.trackable.issue.subject %>
  <% end %>
<% else %>
  changed an issue which has since been removed
<% end %>